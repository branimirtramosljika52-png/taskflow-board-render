{
  "name": "taskflow-board",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "mysql2": "^3.15.2",
    "pg": "^8.16.3"
  },
  "scripts": {
    "build": "npm test && node scripts/build.mjs",
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --test"
  }
}
